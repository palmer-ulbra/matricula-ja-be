# Postgres local
DATABASE_URL=postgres://postgres:postgres@localhost:5432/matriculaja

# Supabase — use o *transaction pooler* (porta 6543) quando rodar na Vercel.
# Conexão direta (5432) esgota o Postgres em função serverless.
# DATABASE_URL=postgres://postgres.<ref>:<senha>@aws-0-<regiao>.pooler.supabase.com:6543/postgres

JWT_SECRET=troque-isto-em-producao
PORT=3000
